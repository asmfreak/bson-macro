#include <stdio.h>
#include "bson.h"

int main(){
    unsigned char a[] = {
//EDITHERE 
    };
    unsigned char b[] = {
    };
    for(int i=0; i<sizeof(a); i++ ){
      printf("\\x%x", (int)a[i]);
    }
    //printf("\n");
}
